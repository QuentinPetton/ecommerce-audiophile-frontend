import { SplitBeforeCategoryPipe } from './split-before-category.pipe';

describe('SplitBeforeCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitBeforeCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
